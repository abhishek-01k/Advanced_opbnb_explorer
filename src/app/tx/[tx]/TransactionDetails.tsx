import React from 'react';

const TransactionDetails = ({
    data
}: any) => {

    console.log("Txn Details >>>>", data);

    return (
        <div>
            <div>Transaction Details Transaction TransactionTransaction Transaction</div>
        </div>
    );
};

export default TransactionDetails;